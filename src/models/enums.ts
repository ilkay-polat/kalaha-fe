export enum FormStatus {
  FIRSTPLAYER,
  SECONDPLAYER,
  LOADING,
  COMPLETED
}

export enum GameStatus {
  CREATED = "Created",
  IN_PROGRESS = "In Progress",
  OVER = "Over",
}

export enum PlayerNo {
  PLAYER_1 = 1,
  PLAYER_2 = 2,
}